import mongoose,{Schema,Types} from 'mongoose';
export interface IAccount extends mongoose.Document {userId:Types.ObjectId;type:string;provider:string;providerAccountId:string;refresh_token?:string;access_token?:string;expires_at?:number;token_type?:string;scope?:string;id_token?:string;session_state?:string}
const S=new Schema<IAccount>({userId:{type:Schema.Types.ObjectId,ref:'User',required:true,index:true},type:String,provider:String,providerAccountId:String,refresh_token:String,access_token:String,expires_at:Number,token_type:String,scope:String,id_token:String,session_state:String},{collection:'accounts',strict:false});S.index({provider:1,providerAccountId:1},{unique:true});export default (mongoose.models.Account as mongoose.Model<IAccount>)||mongoose.model<IAccount>('Account',S);
