import {describe,expect,it} from 'vitest';import {VoiceIntentSchema} from '@/lib/validations/voice';import {normalizeBengaliDigits,normalizeVoiceText} from '@/lib/voice/normalize';
const base={entity_type:'CUSTOMER' as const,entity_name:'রহিম',amount:500,quantity:null,unit:null,transaction_type:'DUE_GIVEN' as const,notes:null,phone:null,buy_price:null,sell_price:null,low_stock_threshold:null,party_type:null};
describe('voice accounting semantics',()=>{it('classifies Rahim due-given payload',()=>{const r=VoiceIntentSchema.safeParse({intent:'CREATE_TRANSACTION',...base});expect(r.success).toBe(true);if(r.success)expect(r.data.transaction_type).toBe('DUE_GIVEN');});it('accepts due received separately',()=>{const r=VoiceIntentSchema.safeParse({...base,intent:'CREATE_TRANSACTION',transaction_type:'DUE_RECEIVED'});expect(r.success).toBe(true);});it('rejects ambiguous sale without product',()=>{const r=VoiceIntentSchema.safeParse({...base,intent:'CREATE_TRANSACTION',entity_name:null,transaction_type:'SALE',amount:null});expect(r.success).toBe(false);});it('normalizes Bangla digits',()=>expect(normalizeBengaliDigits('৫০০ টাকা')).toBe('500 টাকা'));it('normalizes whitespace',()=>expect(normalizeVoiceText('  রহিম   ভাই  ')).toBe('রহিম ভাই'));});
