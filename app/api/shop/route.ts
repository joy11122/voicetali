import {NextResponse} from 'next/server';import {auth} from '@/auth';import {connectDB} from '@/lib/db';import Shop from '@/models/Shop';import {Types} from 'mongoose';import {z} from 'zod';
const schema=z.object({shopName:z.string().trim().min(2).max(160),currency:z.string().trim().min(1).max(20)});
export async function GET(){const s=await auth();if(!s?.user?.id)return NextResponse.json({error:'Unauthorized'},{status:401});await connectDB();return NextResponse.json(await Shop.findOne({userId:new Types.ObjectId(s.user.id)}).lean())}
export async function PATCH(req:Request){const s=await auth();if(!s?.user?.id)return NextResponse.json({error:'Unauthorized'},{status:401});const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:p.error.flatten()},{status:422});await connectDB();return NextResponse.json(await Shop.findOneAndUpdate({userId:new Types.ObjectId(s.user.id)},{$set:p.data},{new:true,upsert:true,setDefaultsOnInsert:true}).lean())}
