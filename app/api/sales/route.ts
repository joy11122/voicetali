import {NextResponse} from "next/server";import {auth} from "@/auth";import {createSale} from "@/services/businessService";import {connectDB} from "@/lib/db";import Transaction from "@/models/Transaction";import {Types} from "mongoose";import {z} from "zod";export async function GET(req:Request){const s=await auth();if(!s?.user?.id)return NextResponse.json({error:"Unauthorized"},{status:401});await connectDB();const u=new URL(req.url);const rows=await Transaction.find({userId:new Types.ObjectId(s.user.id),type:"SALE"}).sort({timestamp:-1}).limit(Math.min(Number(u.searchParams.get("limit")||100),200)).populate("partyId","name").populate("productId","name unit").lean();return NextResponse.json(rows)}export async function POST(req:Request){const s=await auth();if(!s?.user?.id)return NextResponse.json({error:"Unauthorized"},{status:401});try{const b=await req.json();const v=z.object({partyId:z.string().nullable().optional(),productId:z.string().min(1),quantity:z.coerce.number().finite().positive(),unitPrice:z.coerce.number().finite().positive(),paidAmount:z.coerce.number().finite().min(0),notes:z.string().max(500).optional()}).parse(b);const r=await createSale(s.user.id,v);return NextResponse.json({ok:true,result:r},{status:201})}catch(e:any){return NextResponse.json({error:e.message,code:e.code},{status:e.code==="INSUFFICIENT_STOCK"?409:400})}}