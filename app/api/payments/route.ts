import {NextResponse} from "next/server";import {auth} from "@/auth";import {settleParty} from "@/services/businessService";import {connectDB} from "@/lib/db";import Transaction from "@/models/Transaction";import {Types} from "mongoose";export async function GET(){const s=await auth();if(!s?.user?.id)return NextResponse.json({error:"Unauthorized"},{status:401});await connectDB();return NextResponse.json(await Transaction.find({userId:new Types.ObjectId(s.user.id),type:{$in:["DUE_RECEIVED","DUE_GIVEN"]}}).sort({timestamp:-1}).limit(200).populate("partyId","name partyType").lean())}export async function POST(req:Request){const s=await auth();if(!s?.user?.id)return NextResponse.json({error:"Unauthorized"},{status:401});try{const b=await req.json();return NextResponse.json({ok:true,result:await settleParty(s.user.id,{partyId:b.partyId,amount:Number(b.amount),direction:b.direction,notes:b.notes})},{status:201})}catch(e:any){return NextResponse.json({error:e.message,code:e.code},{status:400})}}