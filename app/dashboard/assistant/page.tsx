"use client";import {useState} from "react";export default function Page(){const[q,setQ]=useState(""),[a,setA]=useState("");async function ask(e:any){e.preventDefault();const r=await fetch("/api/assistant",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({question:q})});const d=await r.json();setA(d.answer||d.error||"No answer")}return <div className="mx-auto max-w-3xl"><h1 className="text-3xl font-bold">Smart Shop Assistant</h1><p className="mt-2 text-slate-500">Ask about sales, profit, receivables, payables or stock.</p><form onSubmit={ask} className="mt-6 flex gap-2"><input className="field" value={q} onChange={e=>setQ(e.target.value)} placeholder="আজকের বিক্রি কেমন?"/><button className="btn-primary">Ask</button></form>{a&&<div className="mt-5 rounded-2xl border bg-white p-5 text-lg">{a}</div>}</div>}