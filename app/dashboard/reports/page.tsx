"use client";import {useEffect,useState} from "react";export default function Page(){const[d,setD]=useState<any>();useEffect(()=>{fetch("/api/reports?days=30").then(r=>r.json()).then(setD)},[]);const cards=[['Sales',d?.sales],['COGS',d?.cogs],['Gross profit',d?.grossProfit],['Expenses',d?.expenses],['Net profit',d?.netProfit],['Collections',d?.collections]];return <div className="mx-auto max-w-6xl"><h1 className="text-3xl font-bold">রিপোর্ট</h1><p className="mt-2 text-slate-500">Last 30 days business summary.</p><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{cards.map(([k,v])=><div className="rounded-2xl border bg-white p-5" key={k}><p className="text-sm text-slate-500">{k}</p><p className="mt-2 text-2xl font-bold">৳{Number(v||0).toLocaleString()}</p></div>)}</div><div className="mt-5 rounded-2xl border bg-white p-5">Transactions: {d?.transactions??'—'}</div></div>}