'use client';import {signIn} from 'next-auth/react';import {Chrome} from 'lucide-react';export default function GoogleButton(){if(typeof window!=='undefined'&&document.body.dataset.google==='disabled')return null;return <button type="button" onClick={()=>signIn('google',{callbackUrl:'/dashboard'})} className="flex w-full items-center justify-center gap-2 rounded-xl border bg-white px-4 py-3 font-medium hover:bg-slate-50"><Chrome size={18}/> Continue with Google</button>}
